name: Update README with IPTV Player

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Generate README
        run: |
          echo "# 📺 IPTV MIR 2026" > README.md
          echo "" >> README.md
          echo "### Прямой эфир доступен ниже:" >> README.md
          echo "" >> README.md
          
          # Вставка плеера через iframe
          echo '<iframe src="https://iptvru2026.github.io/IPTVMIR/full.html" width="100%" height="600px" frameborder="0"></iframe>' >> README.md
          
          echo "" >> README.md
          echo '[![Перейти на сайт](https://img.shields.io/badge/Смотреть-На_полный_экран-blue?style=for-the-badge&logo=googlechrome&logoColor=white)](https://iptvru2026.github.io/IPTVMIR/full.html)' >> README.md
          echo "" >> README.md
          echo "---" >> README.md
          echo "### ℹ️ Краткая информация о релизе" >> README.md
          echo "Данный релиз **IPTV MIR** включает в себя обновленный список каналов, поддержку адаптивного плеера и оптимизацию для мобильных устройств. Все потоки обновляются в реальном времени." >> README.md

      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "Update README with latest player" || exit 0
          git push
